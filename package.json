{
  "name": "ajedrez-de-batalla",
  "private": true,
  "version": "1.0.0",
  "description": "Ajedrez 3D vía web inspirado en Battle Chess",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n web,api -c auto \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server",
    "preview": "npm run preview --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
